services:
  dizipal-addon:
    build: .
    container_name: dizipal
    restart: unless-stopped
    ports:
      - "7005:7005"
    env_file:
      - .env
    volumes:
      - ./static/subs:/root/static/subs
